# Allow setup/index.php (the one-time web setup page)
# but deny the CLI-only script from the web.
<Files "create_admin.php">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Deny from all
    </IfModule>
</Files>
